const { getMasterSequelize, getMasterTenant, getTenantModels, closeAllConnections } = require('../config/connectionManager');
module.exports = { getMasterSequelize, getMasterTenant, getTenantModels, closeAllConnections };
